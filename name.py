import os
import pandas as pd
from CTFishPy.CTreader import CTreader

def rename(og, new):
    if path.exists(og):
	# get the path to the file in the current directory
        src = path.realpath(og)
	# rename the original file
        os.rename(og,new) 




